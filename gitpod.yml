tasks:
  - name: name: Use dimod 0.12.12 and run jupyter notebook
    init: |
          sudo pip uninstall -y dwave-ocean-sdk
          sudo pip install -r requirements.txt
    command: |
          jupyter notebook *.ipynb